#include "main.h"
#include <stdlib.h>
#include <string.h>

/**
 * _atoi - function that convert a string to an integer
 *
 * @s: string to be converted to integer
 *
 * Return: no return
 *
 */

int _atoi(char *s)
{
	int val;

	val = atoi(s);

	return (val);
}
